// Bootstrap
import * as bootstrap from 'bootstrap'
import * as Popper from "@popperjs/core"

// Slider
new Swiper('.swiper', {
    pagination: {
        el: '.swiper-pagination',
        clickable: true,
    },
});
